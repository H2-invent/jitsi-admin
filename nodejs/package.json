{
  "name": "jitsi-admin-websocket",
  "version": "1.0.0",
  "description": "",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "test": "WEBSOCKET_SECRET=test-secret-do-not-use-in-production mocha tests/websocket.test.mjs --timeout 15000 --exit",
    "test:ci": "WEBSOCKET_SECRET=test-secret-do-not-use-in-production mocha tests/websocket.test.mjs --timeout 15000 --exit --reporter dot",
    "test:cluster": "WEBSOCKET_SECRET=test-cluster-secret-do-not-use-in-production mocha tests/websocket-cluster.test.mjs --timeout 90000 --exit",
    "test:all": "npm test && npm run test:cluster"
  },
  "author": "H2 invent GmbH",
  "license": "AGPLv3",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "socket.io": "^4.5.1",
    "redis": "^5.3.1",
    "@socket.io/redis-adapter": "^8.2.0"
  },
  "devDependencies": {
    "chai": "^5.1.0",
    "mocha": "^11.1.0",
    "socket.io-client": "^4.6.1"
  }
}

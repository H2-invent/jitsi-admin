services:
    App\Tests\Functional\Fixtures\HubStub:
        decorates: mercure.hub.default

parameters:
    ldap_test_url: '%env(ldap_test_url)%'
    encryption.private_key_path: '%kernel.project_dir%/tests/Provisioner/backend_private.pem'
    encryption.public_key_path: '%kernel.project_dir%/tests/Provisioner/backend_public.pem'

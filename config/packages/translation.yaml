framework:
    default_locale: de
    translator:
        default_path: '%kernel.project_dir%/translations'
        path:
            - '%kernel.project_dir%/translations/custom'
        fallbacks:
            - de

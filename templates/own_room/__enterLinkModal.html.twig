<div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">

        <div class="modal-header  light-blue darken-3 white-text">
            <h5 class="modal-title">{{ 'Einladungslinks'|trans }}</h5>
            <button type="button" class="btn-close" data-mdb-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
            <h5>{{ 'Beitretenlink für Teilnehmer'|trans }}</h5>
            <div class="md-form input-with-post-icon">

            </div>

            <div class="input-group mb-3">
                <input id="linkPart" readonly type="text" class="form-control"
                       value="{{ httpsAbolutUrl(path('own_room_startPage',{'uid':room.uid})) }}">

                <button class="btn btn-md btn-outline-primary waves-effect copyLink" type="button"
                        data-clipboard-target="#linkPart"><i class="fa-regular fa-copy"></i></button>

            </div>

                <div class="row">

                    <div class="col-lg-12">
                        <p>{{ 'openRoom.addParticipants.help'|trans }}</p>
                        <a href="{{ path('dashboard_api_participants',{'room':room.id}) }}"
                           data-manage-participants="{{ path('dashboard_api_participants',{'room':room.id}) }}"
                           data-room-name="{{ room.name }}"
                           class=" element btn btn-outline-default"><i
                                    class="fas fa-plus"></i> {{ 'Teilnehmer'|trans }}</a>
                    </div>
                </div>

        </div>
    </div>
</div>

<div class="col-xs-12 col-sm-6 col-md-4  col-lg-3 col-xl-4 mb-3 favorites">
    <div class="card text-center h-100">

        <div class="card-body d-flex flex-column justify-content-between">
            <h5 class="card-title">
                {{ room.name }}
            </h5>
            {% if room.persistantRoom == false %}
<small>{{ room.start|date('d.m.Y H:i') }}</small><br>
            {% endif %}
            {% if room in runningRooms %}<small class="badge badge-success" {% if theme %} style="background-color: {{ theme.colorBadgeRunningNow }} !important;" {% endif %}>{{ 'Läuft gerade'|trans }}</small><br>{% endif %}
            <div class="test">
                <a class="btn btn-outline-primary dropdown-toggle btn-sm" type="button" data-toggle="dropdown" aria-haspopup="true"
                   aria-expanded="false"><i class="fa fa-play"></i></a>

                <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('room_join',{'room':room.id,'t':'b'}) }}" target="_blank">{{ 'Im Browser'|trans }}</a>
                    <a class="dropdown-item" href="{{ path('room_join',{'room':room.id,'t':'a'}) }}" target="_blank">{{ 'In der App'|trans }}</a>
                </div>
            </div>
        </div>


    </div>
</div>
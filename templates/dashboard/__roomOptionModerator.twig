{% if room.moderator == app.user %}
    {{ include('dashboard/__roomOption.html.twig') }}
{% endif %}

{% if room.moderator == app.user and room.totalOpenRooms != true %}

    <a href="{{ path('room_add_user',{'room':room.id}) }}"
       class=" element btn btn-outline-primary loadContent"><i class="fa-solid fa-users"></i></a>
{% endif %}
{% if room.moderator == app.user and room.totalOpenRooms == true %}
    <a href="{{ path('room_enter_link',{'uid':room.uid}) }}"
       class=" element btn btn-outline-primary loadContent">{{ 'Beitretenlink'|trans }}</a>
{% endif %}



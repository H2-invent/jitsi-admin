<div class="mt-5 removeInIframe text-center text-black-50 small">

    {% if theme == false or (theme is not null and theme.allowHomePage == true) %}
            <a href="{{ path('index') }}" class="text-black-50 small">{{ 'Startseite'|trans }}</a>
    {% endif %}
    {% if theme == false or (theme is not null and theme.allowDashboard == true) %}
            | <a href="{{ path('dashboard') }}" class="text-black-50 small">{{ 'Dashboard'|trans }}</a>
    {% endif %}
    {% if theme == false or (theme is not null and theme.allowRegister == true) %}
            | <a href="{{ path('register_keycloak') }}" class="text-black-50 small">{{ 'Registrieren'|trans }}</a>
    {% endif %}
    {% if theme == false or (theme is not null and theme.allowHelp == true) %}
            | <a href="{{ helpUrlParticipants }}" class="text-black-50 small" target="_blank">{{ 'Hilfe'|trans }}</a>
    {% endif %}
</div>

{% extends 'join/base.html.twig' %}

{% block content %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="card card-body">
        <h1 class="h1-responsive text-center">Konferenz beitreten</h1>
        <hr>
        <div class="text-center">
        </div>
        <div class="row mb-5">
            <div class="col-md-6">
                <a href="#" class="btn btn-primary">Eigene Konferenz
                    erstellen</a>
            </div>
            <div class="col-md-6">
                <p>
                    Füllen Sie das Formular aus um einer Konferenz beizutreten
                </p>

                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>

    </div>
    {{ include('base/__snack.html.twig') }}
{% endblock %}

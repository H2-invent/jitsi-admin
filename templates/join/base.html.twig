<!DOCTYPE html>
<html>

{{ include('base/__head.html.twig') }}

<body style="background-color: #C9C9C9">
<div class="imageBackground"></div>
{% block back %}{% endblock %}
<div>
    <div class="container mb-5">
        <div class="pt-5">
            {% block content %}{% endblock %}
        </div>
        <div class="row justify-content-center ">
            <div class="col-12">
                <div class="card card-body mt-4 text-center">
                    <div class="row">
                        <div class="col-md-3 text-md-left">

                            {% if server is defined and  server is not null and server|validateLicense %}

                            {% else %}
                                <a href="https://adm.saza.vn" target="_blank" class="small"><img
                                            src="{{ asset('logo.svg') }}" style="height: 50px"></a>
                            {% endif %}
                        </div>
                        <div class="col-md-6">
                            <a href="{{ dataPrivacy }}" target="_blank"
                               class="small">{{ 'Datenschutzhinweis'|trans }}</a><br>
                            <a href="https://adm.saza.vn" target="_blank" class="small">Powered by Meet
                                Admin</a><br>
                            {% if matomo_enable == 'true' %}
                                <a class="small" href="#" data-toggle="modal" data-target="#exampleModal">
                                    Matomo Opt-out
                                </a><br>
                            {% endif %}
                            <a href="https://adm.saza.vn" target="_blank" class="small">Made with ❤ by NAMTEL •
                                2019-{{ 'now'|date('Y') }}
                                • </a>
                        </div>
                        <div class="col-md-2 text-md-center d-none d-sm-block">
                            <a href="https://github.com/namtel-hp/admin/blob/master/LICENSE" target="_blank" referrerpolicy="no-referrer" rel="noopener"> <img src="{{ asset('images/agpl.png') }}" style="width: 100%"></a>
                        </div>
                        <div class="col-md-1 text-md-right d-none d-sm-block">
                            <a href="https://opensource.org/" referrerpolicy="no-referrer" rel="noopener"><img src="{{ asset('images/open-source-logo.png') }}" style="width: 100%"></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>

    {{ include('base/__matomoOptOut.html.twig') }}
</div>

{% block webpack %}
    {{ encore_entry_script_tags('frontend') }}
{% endblock %}


</body>

</html>
